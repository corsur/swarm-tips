# PulseMCP outreach — partner API access + listing

**Status:** DRAFT — do not send until MCP server is stable (clean test run + ~24h with no new bugs).
**To:** `api@pulsemcp.com`
**Cc:** `hello@pulsemcp.com`
**From:** `<FROM ADDRESS / X handle — fill in before sending>`
**Subject:** Swarm Tips — Sub-Registry API access + listing for `io.github.corsur/swarm-tips`

**Note:** PulseMCP's published API docs (verified 2026-04-07) confirm the Sub-Registry API requires `X-API-Key` + `X-Tenant-ID` headers, both provisioned by emailing `api@pulsemcp.com`. Authenticated rate limits are 200/min, 5K/hr, 10K/day — we expect to use ~20 requests/day (one daily refresh × ~20 paginated `?version=latest&limit=100` calls). Sending the API ask directly to `api@` and CC'ing `hello@` for the editorial side.

---

Hi PulseMCP team,

I'm writing on behalf of Swarm Tips (`swarm.tips`), a DAO building coordination infrastructure for AI agents. We've published `io.github.corsur/swarm-tips` to the official MCP registry — it's a unified MCP server (`https://mcp.swarm.tips/mcp`, Streamable HTTP) exposing 28 tools across four live mainnet protocols:

- **Coordination Game** (`coordination.game`) — non-custodial 1v1 social-deduction game on Solana, real SOL stakes, agents sign locally
- **Shillbot** (`shillbot.org`) — autonomous short-form video marketplace, USDC-escrowed, Switchboard-oracle-verified
- **ClawTasks + BotBounty proxies** — agent bounty marketplaces on Base
- **Video generation via x402** — 5 USDC per short, multi-chain

Two related asks:

1. **Sub-Registry API credentials.** We're consuming PulseMCP as one of four Layer 1 sources in our discovery pipeline (`src/discovery/sources.rs`), alongside the official MCP registry, `wong2/awesome-mcp-servers`, and `tolkonepiu/best-of-mcp-servers`. Could you provision an `X-API-Key` + `X-Tenant-ID` for us against `https://www.pulsemcp.com/api/docs/v0.1`? Expected volume: **~20 requests/day** (one daily discovery refresh, paginated `GET /v0.1/servers?version=latest&limit=100`). Well below the 10K/day rate limit. We're happy to share full attribution — every PulseMCP-sourced entry in our catalog is tagged with the source label and links back to the canonical PulseMCP listing.

2. **Directory listing.** I'm submitting `io.github.corsur/swarm-tips` via your `/submit` form in parallel. If your editorial team wants to take a closer look — we believe Swarm Tips is the only MCP server today that lets a single agent earn real revenue across four independent mainnet protocols through one connection — we'd be glad to walk through end-to-end flows on a sandbox wallet.

Reference links:
- Official registry entry: `https://registry.modelcontextprotocol.io/v0/servers/io.github.corsur~swarm-tips`
- A2A agent card: `https://swarm.tips/.well-known/agent.json`
- llms.txt: `https://swarm.tips/llms.txt`
- GitHub: `https://github.com/corsur/swarm-tips`
- Developer docs: `https://swarm.tips/developers`

Thanks for the work you're doing on the registry,
Swarm Tips

---

## Send-checklist (do these in order when MCP is stable)

1. Confirm `io.github.corsur/swarm-tips` is at v0.1.2 on `registry.modelcontextprotocol.io` with the new "28 tools…non-custodial" description.
2. Submit the public listing form at `https://www.pulsemcp.com/submit` with the registry name + GitHub link.
3. Send this email to `api@pulsemcp.com` with `hello@pulsemcp.com` on CC. Fill in the FROM line.
4. Track replies; expect a response within ~5 business days.
5. **When credentials arrive:** create two GCP Secret Manager secrets in the mcp-server project, `pulsemcp-api-key` and `pulsemcp-tenant-id`, then implement `pull_pulsemcp(...)` in `src/discovery/sources.rs` (the implementation slot is documented in `glistening-humming-treasure.md` step 5). Code can land before credentials — `load_optional_secret` makes the source no-op gracefully when secrets are absent.
