name: Program

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev

      - name: Install Solana CLI
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Install Anchor CLI
        run: cargo install --git https://github.com/coral-xyz/anchor --tag v0.32.1 anchor-cli --locked

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: cargo fmt --check

      - run: cargo clippy --all-targets -- -D warnings

      - name: Generate throwaway keypair for local validator
        run: |
          mkdir -p ~/.config/solana
          solana-keygen new --no-bip39-passphrase --silent -o ~/.config/solana/id.json

      - name: Build programs individually (shared crate is a library, not a program)
        run: |
          anchor build -p coordination
          anchor build -p shillbot

      - run: npm run lint

      - run: anchor test

      - name: Stop local validator
        if: always()
        run: pkill -f solana-test-validator || true

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev

      - name: Install Solana CLI
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/stable/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      - name: Install Anchor CLI
        run: cargo install --git https://github.com/coral-xyz/anchor --tag v0.32.1 anchor-cli --locked

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://npm.pkg.github.com'

      - run: npm ci

      - name: Write deploy keypair
        run: |
          mkdir -p ~/.config/solana
          echo '${{ secrets.SOLANA_DEPLOY_KEYPAIR }}' > ~/.config/solana/id.json

      - name: Write program keypairs
        run: |
          mkdir -p target/deploy
          echo '${{ secrets.ANCHOR_PROGRAM_KEYPAIR }}' > target/deploy/coordination-keypair.json
          echo '${{ secrets.SHILLBOT_PROGRAM_KEYPAIR }}' > target/deploy/shillbot-keypair.json

      - name: Build programs individually
        run: |
          anchor build -p coordination
          anchor build -p shillbot

      - name: Close orphaned buffer accounts
        run: |
          # Failed deploys leave behind funded buffer accounts whose SOL is locked in
          # the wallet. Close them before deploying to recover those lamports.
          solana program close --buffers --url devnet --keypair ~/.config/solana/id.json || true

      - name: Deploy programs to devnet
        run: |
          anchor deploy -p coordination --provider.cluster devnet
          anchor deploy -p shillbot --provider.cluster devnet

      - name: Publish SDK to GitHub Packages
        run: |
          # Coordination SDK
          cp target/types/coordination.ts sdk/coordination.ts
          node -e "
            const idl = require('./target/idl/coordination.json');
            const fs = require('fs');
            fs.appendFileSync('sdk/coordination.ts',
              '\nexport const IDL: Coordination = ' + JSON.stringify(idl, null, 2) + ' as unknown as Coordination;\n'
            );
          "
          # Shillbot SDK
          cp target/types/shillbot.ts sdk/shillbot.ts
          node -e "
            const idl = require('./target/idl/shillbot.json');
            const fs = require('fs');
            fs.appendFileSync('sdk/shillbot.ts',
              '\nexport const IDL: Shillbot = ' + JSON.stringify(idl, null, 2) + ' as unknown as Shillbot;\n'
            );
          "
          cd sdk
          npm version 0.0.0-$(git rev-parse --short HEAD) --no-git-tag-version
          npm publish --tag latest
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
